# Roadmap

## v0.1

- FastMCP stdio server
- Mock graph (topics/nodes/services/params)
- Live bridge via `ros2` CLI
- CLI demo + tests

## v0.2

- Optional `rclpy` backend (no CLI parsing)
- Action servers list/send goal
- TF tree snapshot tool
- Streaming topic echo (session resources)

## v0.3

- Bridge to Lappa native sim HTTP
- Secure allowlist for live publish topics
- Docker image with ROS2 Humble + MCP
