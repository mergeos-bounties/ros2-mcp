# Paste into ~/.grok/config.toml or project .grok/config.toml
# Requires: pip install "git+https://github.com/mergeos-bounties/ros2-mcp.git"

[mcp_servers.ros2_mcp]
command = "ros2-mcp"
args = ["serve"]
env = { ROS2_MCP_MODE = "mock" }
enabled = true
